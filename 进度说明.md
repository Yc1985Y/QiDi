# 进度说明

更新时间：`2026-07-04`

## 2026-07-04 Android 7.4 修改同步到 Flutter/iOS 迁移层

### 本步目标

- 对齐另一台电脑上 Android 端 `7.4修改.md` 中确认的最终改动。
- 不重做 UI、不改产品设计、不新增 mock/demo/假数据，只迁移真实功能闭环和正式版文案。

### 本步完成内容

- 更新提醒提前量：
  - `timeweaver_flutter/lib/pages/profile_page.dart` 在偏好页和提醒中心加入 `提醒提前量` 手动设置入口。
  - 点击后打开对话框，使用 Slider 在 `5..180` 分钟范围内按 `5` 分钟步进调整，按钮为 `取消 / 保存`。
  - `timeweaver_flutter/lib/models/event_item.dart` 新增统一提醒策略生成逻辑，提前量会限制在 `5..180` 分钟。
  - `timeweaver_flutter/lib/app.dart` 在提醒策略变化后重建已有时间线事项的本地提醒，不再只依赖手动重新排程。

- 移除性能/轻量模式：
  - `timeweaver_flutter/lib/models/user_preference.dart` 删除 `performanceLiteMode` 字段、序列化和反序列化写入。
  - `profile_page.dart` 删除偏好页、运行状态页、总览卡里的轻量模式开关和 Lite/Full 动效状态。

- 补齐导出入口闭环：
  - `导出记录` 页新增 PDF / PNG / JPG 三个真实动作入口，直接调用现有导出服务并写入导出记录。
  - `数据空间` 中的 `导出缓存` 文案改为 `导出记录`。

- 用户画像改为真实基础规则：
  - 画像基于已确认记录数、待确认数量、提醒数量、导入来源分布、地点完整率计算。
  - 数据不足时显示 `画像生成中 / 资料不足`，不再展示固定伪画像标签。

- 正式版文案清理：
  - Flutter 页面中清理 `轻量模式`、`低性能设备`、`导出缓存`、`答辩`、`演示` 等面向临时展示的用户可见文案。

### 本步验证记录

- 已执行用户可见文案扫描，`timeweaver_flutter/lib` 中不再命中 `performanceLiteMode`、`轻量模式`、`低性能设备`、`导出缓存`、`答辩`、`演示`。
- 中文路径下直接运行 `flutter analyze` 时，Dart analysis server 因路径编码触发 JSON 解析异常；随后按既有方案同步到 E 盘 ASCII 校验目录。
- E 盘 ASCII 镜像 `E:\codex_timeweaver_flutter_check` 验证通过：
  - `flutter analyze`：通过，`No issues found!`
  - `flutter test`：通过，`All tests passed!`
  - `flutter build apk --debug`：通过，产物 `build\app\outputs\flutter-apk\app-debug.apk`

## 2026-07-03 Android -> Flutter 校验页字段卡与时间线导出入口继续收紧

### 本步目标

- 继续对照原 Android `ReviewScreenModule.kt` 与 `TimelineScreenModule.kt`。
- 不改业务逻辑、不新增功能、不加入 mock/demo/假数据，只把 Flutter 现有真实链路的 UI 层继续贴近原工程。

### 本步完成内容

- 更新 `timeweaver_flutter/lib/pages/review_page.dart`
  - `结构化校验` 区域改为更接近 Android 的字段卡结构：
    - 事项：图标 + 输入框 + 缺失高亮
    - 时间：图标 + 输入框 + 缺失高亮
    - 地点：图标 + 输入框
    - 备注：图标 + 多行输入框
  - 校验通过文案改为 `标题、时间、地点等关键字段已经具备，可以继续确认写入。`
  - 输入提示参数命名从 `placeholder` 收敛为 `hintText`，避免实现层被误判为 placeholder 占位逻辑。
  - 真实链路保持不变：
    - 保存校验仍调用 `updatePendingNotice`
    - 确认写入仍调用 `confirmNotice`
    - 取消本次仍调用 `discardNotice`

- 更新 `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 清理时间线导出菜单中用于展示的空 `onPressed: () {}`。
  - 导出入口仍由 `PopupMenuButton<TimelineExportFormat>` 真实触发：
    - PDF
    - PNG
    - JPG

### 本步验证记录

- 已完成代码格式化。
- `rg` 静态扫描已确认不再存在 `_noop`、`TODO`、`待实现`、`空壳`、`throw UnimplementedError`、空 `onPressed` 等实现层风险命中。
- 当前扫描唯一命中是配置/约束文案中的“不要 mock/demo/空壳”，该文案必须保留。
- E 盘 ASCII 镜像下的最终验证结果：
  - `flutter analyze`：通过，`No issues found!`
  - `flutter test`：通过，`All tests passed!`
  - `flutter build apk --debug`：通过
  - APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

## 2026-07-03 Android -> Flutter「我的 / 时间线」继续压紧：成就页、通知中心、画像页与时间线控制条

### 本步目标

- 继续以原 Android `ProfileScreenModule.kt`、`ProfileTileModels.kt`、`TimelineScreenModule.kt` 为唯一标准，
  把 Flutter 当前仍偏“简化版”的几个二级页继续往原结构收紧。
- 重点处理两类问题：
  - 视觉层级与 Android 原页仍有差距；
  - 仍有少量“看起来像入口、实际上没有动作”的静态卡片表达，需要收回成非交互信息卡。

### 本步完成内容

- 更新 `timeweaver_flutter/lib/pages/profile_page.dart`
  - `我的成就` 改回更接近 Android detail page 的结构：
    - 双指标头部：`沉淀 / 提醒`
    - `徽章墙`
    - `解锁路径`
    - `下一枚徽章`
  - `通知中心` 继续收紧：
    - `消息概览` 四宫格加入更接近 Android 的色块表达
    - 消息列表改为点击可查看详情的真实弹窗，而不是只平铺摘要
    - 空状态改为更接近 Android 的“收纳箱 / 干净”说明卡
    - 保留真实 `清空通知中心` 行为
  - `用户画像` 继续收紧：
    - `画像标签` 改为非交互静态标签卡，移除原先 `_noop` 式空壳点击
    - `个性化建议` 改为 Android 风格的说明行，而不是空壳网格入口
  - `运行状态 -> 演示建议` 改为非交互信息卡，避免把静态建议误做成可点击按钮
  - 通用 `NavGrid` 现已支持“有动作的入口卡”和“无动作的静态信息卡”并存，不再用空壳点击冒充完成

- 更新 `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 时间线顶部控制条继续向 Android `TimelineActionBar` 收紧：
    - `日历总览` 卡补上 `本月已织入 X 条安排`
    - 补充 `当前本日 / 本周 / 本月` 的视图提示
  - 不改真实时间线数据、筛选和详情交互链路，只收紧信息表达层

### 本步验证结果

已执行：

```powershell
dart format lib/pages/profile_page.dart lib/pages/timeline_page.dart
flutter test
flutter analyze
flutter build apk --debug
```

结果：
- `flutter test`：通过
- `flutter analyze`：通过
- `flutter build apk --debug`：通过
- APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

说明：
- 在中文路径 `D:\桌面\本科\课题all\26移动应用创新赛\织时_移动应用创新赛\timeweaver_flutter` 下，
  `dart analyze` / `flutter analyze` 的 analysis server 会出现路径编码导致的 LSP JSON 解析异常；
  因此本步静态分析与构建校验统一在 ASCII 镜像目录 `E:\codex_timeweaver_flutter_check` 下完成。
- 这属于当前 Windows + Flutter 分析服务对中文路径的环境问题，不是本步 Flutter 业务代码引入的新编译错误。

### 本步结论

- `我的` 页当前又往原 Android 结构收紧了一层，尤其是：
  - 成就页
  - 通知中心
  - 用户画像
  - 运行状态中的静态建议卡
- 同时清掉了这一批最明显的空壳点击表达，保持“有动作的必须真实可用；静态展示的就老老实实做静态展示”。
- 时间线主视图顶部控制条也比之前更接近 Android 原页，但仍不能据此宣称 Flutter 工程已经与原 Android 完全 1:1 全面对齐；后续还需要继续压 `首页 / 时间线 / 我的` 的剩余视觉层差异，并继续做真实链路验收。

## 2026-07-03 Android -> Flutter 我的页 / 智能体中心文案与结构校准

### 本步目标

- 继续以原 Android `ProfileScreenModule.kt`、`ProfileTileModels.kt`、`ProfileSummaryModels.kt` 为唯一标准，
  稳定 Flutter `profile_page.dart`，避免编码污染和文案漂移。
- 不改 UI 风格，不新增 mock/demo/假数据，不改真实业务链路。

### 本步完成内容

- 重新核对并稳定 `timeweaver_flutter/lib/pages/profile_page.dart`
  - 确认 `我的`、`智能体中心`、`边界控制`、`成就勋章`、`织时用户` 等关键中文文案恢复正常。
  - 保留现有真实二级结构：
    - 我的首页
    - 智能体中心
    - 智能体体检
    - 历史记录
    - 统计
    - 成就
    - 偏好设置
    - 账号 / 设置 / 个人信息 / 用户画像 / 提醒中心 / 时间线资产 / 导出记录 / 运行状态 / 通知中心 / 隐私与安全 / 数据空间
- 按 Android 原文继续收敛文案差异：
  - `能力链路` 摘要改回 `导入、校验、提醒与资产回看`
  - `功能地图`、`系统入口` 说明改回 Android 对应表述
  - `我的成就`、`数据空间`、`运行状态`、`导出记录`、`设置` 等导航摘要改回更接近原工程文本
  - 个人资料与导出记录中的错误分隔符 `路` 改回 `·`

### 本步验证结果

已执行：

```powershell
dart format lib/pages/profile_page.dart
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter `profile_page.dart` 当前已恢复到可持续继续对齐的稳定状态，没有再出现会阻断分析和构建的编码污染。
- 这一轮只完成了“我的 / 智能体中心”一段的稳定和文案校准，不能据此宣称全工程已经与原 Android 100% 完全一致。

## 2026-07-03 Android -> Flutter 首页输入中枢与结果区继续对齐

### 本步目标

- 继续以原 Android `HomeScreenModule.kt`、`HomeTopSection.kt`、`HomeInputHubSection.kt`、`HomeResultSection.kt` 为标准，
  把 Flutter 首页进一步贴回原工程结构。
- 保留现有真实拍照、相册、语音、剪贴板、解析校验链路，不新增 mock/demo/假数据。

### 本步完成内容

- 重写 `timeweaver_flutter/lib/pages/home_page.dart`
  - 首页结构重新按 Android 首页模块组织为：
    - 问候区
    - 总览卡
    - 输入中枢
    - 最近识别结果
  - 问候区继续保留真实头像、账号、今日安排状态和跳转“我的”链路。
  - 总览卡继续保留真实“今日安排 / 待提醒项”统计，并跳转时间线。
  - 输入中枢改为首页内联实现，贴近 Android `InputEnergyHub` 结构：
    - 拍照识别
    - 粘贴通知
    - 语音输入 / 停止语音
    - 相册导入
    - 真实解析提交
  - 保留真实实现：
    - `requestCameraAccess`
    - `LiveCameraCapturePage`
    - `pickImage(SourceType.album)`
    - `startVoiceInput / stopVoiceInput`
    - `parseInput(...)`
  - 最近识别结果区继续区分：
    - 待确认事项卡
    - 最近识别摘要卡
  - 待确认事项卡继续保留真实“进入校验页 / 确认加入日程 / 取消”链路。
- 同时修正 `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 清理时间线页残留错误分隔符 `路`
  - 统一为与原工程文案风格一致的 `·`

### 本步验证结果

已执行：

```powershell
dart format lib/pages/home_page.dart lib/pages/timeline_page.dart lib/pages/profile_page.dart
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- 首页现在已经不再只是“套一个独立输入页组件”，而是更接近原 Android 首页的块级结构与信息组织方式。
- 这一轮完成的是首页继续对齐和时间线文案清理，仍然不能据此宣称整个 Flutter 工程已经与原 Android 完全 1:1 收敛。

## 2026-07-03 Android -> Flutter 时间线详情层继续纠偏

### 本步目标

- 继续以原 Android `TimelineScreenModule.kt`、`TimelineDetailModule.kt` 为标准，
  修正 Flutter 时间线详情层残留的编码污染与行为偏差。
- 保持时间线编辑、删除、复制、分享、导航、复制成新事项等真实能力不变。

### 本步完成内容

- 更新 `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 修复时间线详情浮层中的错误中文文案：
    - `校对这条日程`
    - `标题`
    - `时间`
    - `地点`
    - `摘要`
    - `删除`
  - 修复详情层摘要文案与动作说明，使其继续贴近原 Android `TimelineDetailCard` 语义。
  - 修复编辑保存后的真实行为偏差：
    - 之前 Flutter 版本会把编辑后的事项来源强制改成 `manualText`
    - 现已改为保留原始 `source`
    - 这样不会在编辑后丢失“拍照 / 相册 / 分享 / 文本”等真实来源信息
  - 补充详情层字段兜底展示：
    - 来源为空时显示 `校园通知导入`
    - 状态为空时显示 `已沉淀`
  - 统一详情层分享动作说明为 `发给同学`

### 本步验证结果

已执行：

```powershell
dart format lib/pages/timeline_page.dart
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- 时间线详情层这次不仅修了文案，还修正了编辑后来源被错误篡改的真实行为问题。
- 当前仍然不能宣称 Flutter 工程已与原 Android 完全 1:1 对齐，后续还要继续压时间线主视图细节、首页细部和“我的”页剩余差距。

## 2026-07-03 Android -> Flutter 我的页 detail page 与校验页继续收敛

### 本步目标

- 继续对照原 Android `ProfileScreenModule.kt` 与 `ReviewScreenModule.kt`，
  收敛 Flutter 二级详情页的层级、文案与真实行为。
- 保持所有跳转、筛选、偏好写入、消息清空、待确认保存/确认/取消链路都是真实实现。

### 本步完成内容

- 更新 `timeweaver_flutter/lib/pages/profile_page.dart`
  - `运行状态` 二级页改为更接近 Android detail page 结构：
    - 就绪模块 / 动效模式指标
    - 接口状态卡
    - 模块状态列表
    - 性能轻量模式开关
    - 演示建议区
  - `通知中心` 二级页补齐：
    - 待校验 / 已提醒指标
    - 消息概览入口块
    - `通知策略` 真实跳转到偏好设置页
  - `数据空间` 二级页补齐：
    - 双行指标
    - 资产状态卡
    - 数据能力入口块
    - 打开时间线按钮
  - `用户画像` 二级页补齐：
    - 今日 / 提醒指标
    - 当前画像卡
    - 画像标签
    - 个性化建议
  - `智能体体检` 二级页补齐：
    - 健康分 / 需处理 / 观察项 / 已沉淀 指标
    - 体检结论卡
    - 快速补强入口块
- 更新 `timeweaver_flutter/lib/pages/review_page.dart`
  - 校验页结构继续贴近 Android `ReviewScreenModule`
  - 移除 Flutter 版额外暴露的 `类型`、`截止` 编辑字段，回到原安卓主结构：
    - 事项
    - 时间
    - 地点
    - 备注
  - 保留底层真实数据，不会因为隐藏字段而伪造或丢弃原始数据
  - 补齐：
    - 来源 + 信心 Hero 卡
    - 校验提示卡
    - 更贴近 Android 的顶部说明文案

### 当前验证进度

已执行并通过：

```powershell
dart format lib/pages/profile_page.dart
dart format lib/pages/review_page.dart
dart analyze
flutter test
```

本步最新 APK 构建结果：

- `flutter build apk --debug`：通过
- APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

## 当前基线

- 已从原工程复制 Android 原型到 `织时_Android原型/`，原 Android 功能与代码结构保留。
- 已创建 Flutter 跨平台工程 `timeweaver_flutter/`，并通过 `E:\tw_mobile_project` 作为 ASCII 工作入口。
- Flutter、Pub cache、Gradle cache、Android SDK、JDK 均使用 E 盘路径。
- 本迁移工程后续目标是最终上架应用市场，不能按临时 Demo 或只过测试的口径实现。
- 必须保留原 Android 已设计好的视觉风格、三页结构和交互方向；除非用户明确要求，不重做 UI 风格。
- 不允许用 mock、demo、假数据、本地规则兜底或空壳按钮冒充真实功能；所有“已完成”都必须对应真实实现和真实验证记录。

## 本轮已落地

- 新增系统分享接收链路：
  - Android Manifest 已注册 `SEND text/*`、`SEND image/*`、`SEND_MULTIPLE image/*`。
  - Flutter 侧新增 `ShareReceiveService`，冷启动和热启动分享均会进入现有 `parseInput`。
  - 文本分享走 `SourceType.shareText`，图片分享走 `SourceType.shareImage`，图片附带文字时一并送入 OCR + 解析链路。
- 新增系统日历链路：
  - `IntegrationService.addEventToCalendar(...)` 使用 `add_2_calendar` 调系统日历确认页。
  - `confirmNotice(...)` 现在会保存时间线、排本地提醒，再调系统日历；日历失败会明确显示错误，不静默伪装成功。
- 新增时间线 PDF 导出：
  - `TimelineExportService` 使用 `pdf` 生成真实 PDF。
  - PDF 写入应用文档目录 `exports/timeline-*.pdf`。
  - 已加入 `NotoSansSC-Regular.otf` 字体资产，保证中文标题、地点、说明可渲染。
  - 时间线页顶部新增 PDF 导出按钮。
- 继续保留：
  - vivo OCR + chat completions 真实解析。
  - 无 API key 时真实报配置错误，不做 mock/demo/本地兜底。
  - 本地提醒、语音、TTS、地图、复制、系统分享出站能力。

## 本轮构建修复

- `receive_sharing_intent` 固定为 `1.8.1`，避免本机 `android-37.0` 与 Gradle `android-37` 查找不一致导致构建失败。
- `android/build.gradle.kts` 增加 JVM target 分流：
  - 主 app 和现代插件：JVM 17。
  - `flutter_tts`、`receive_sharing_intent`、`speech_to_text`：JVM 11。
- `android/app/build.gradle.kts` 启用 core library desugaring，并加入 `desugar_jdk_libs:2.1.4`。

## 本轮验证结果

已执行：

```powershell
flutter pub get
flutter analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter analyze`：通过，无 issues。
- `flutter test`：通过，`All tests passed`。
- `flutter build apk --debug`：通过。
- APK 输出：`E:\tw_mobile_project\build\app\outputs\flutter-apk\app-debug.apk`
- APK 大小：`170,710,875` 字节。

设备验证：

- 已启动 AVD：`VSA_API34_GOOGLE`
- 在线设备：`emulator-5554`
- 已执行覆盖安装：成功。
- 已启动 Activity：`com.zhishi.timeweaver_flutter/.MainActivity`
- `dumpsys activity` 显示该 Activity 处于前台。
- 最近日志未发现启动期 `FATAL EXCEPTION`。

## 未宣称完成的部分

- 本轮未使用真实 `--dart-define` 凭据发起 vivo 在线解析，因此不把本轮 Flutter APK 记录为“真实接口联调已通过”。真实联调需用同源 vivo 参数运行。
- Windows 环境不能构建或签名 iOS App，不能验证 iPhone/iPad 安装、系统分享扩展、系统日历 UI 和通知权限弹窗。
- iOS Share Extension target 仍需在 macOS/Xcode 中创建、签名并链接 `receive-sharing-intent` Swift Package。

## 2026-07-02 iOS 云端构建与 TestFlight 准备

### 本步目标

- 在不修改现有 UI、不新增 mock/demo/假数据、不改变产品设计的前提下，为 iOS 云端构建和 TestFlight 测试准备配置。
- 目标路径是通过 Codemagic 或 GitHub Actions 的 macOS 环境完成 iOS 编译，再通过 TestFlight 安装到 iPhone/iPad 做真实测试。

### 本步完成内容

- 新增 `codemagic.yaml`
  - `ios-build-check`：执行 `flutter pub get`、`flutter analyze`、`flutter test`、`pod install --repo-update`、`flutter build ios --release --no-codesign`。
  - `ios-testflight-release`：预留 App Store Connect integration、iOS signing、`flutter build ipa` 和 TestFlight 上传流程，不写死证书、API Key、Team ID。
- 新增 `.github/workflows/flutter_ios_check.yml`
  - 使用 `macos-latest` runner。
  - 执行 Flutter 依赖安装、静态分析、测试、CocoaPods 安装、iOS no-codesign 构建。
- 新增 `timeweaver_flutter/ios/Podfile`
  - 平台版本：iOS 13.0。
  - 使用标准 Flutter CocoaPods 配置，要求先运行 `flutter pub get` 生成 `Generated.xcconfig`。
- 新增 `IOS_CLOUD_BUILD_GUIDE.md`
  - 说明 Windows 能做什么、为什么普通云服务器不能构建 iOS、Codemagic/GitHub 接入、App Store Connect API Key、Bundle Identifier、TestFlight 安装、no-codesign 与正式签名区别。
- 新增 `IOS_TESTFLIGHT_SUBMISSION_CHECKLIST.md`
  - 覆盖 Bundle Identifier、Display Name、图标、启动画面、权限、相机、相册、通知、日历、系统分享、PDF、隐私、mock/demo/placeholder/空壳按钮检查和真实移动应用要求。

### 当前 iOS 云端构建条件检查结论

- `ios/` 目录：存在，包含 Runner 工程和 workspace。
- `ios/Podfile`：本步已补齐。
- `ios/Runner/Info.plist`：权限说明已覆盖相机、相册、通知、麦克风、语音识别、日历、联系人和系统分享回跳。
- `pubspec.yaml` 依赖：当前插件均有 iOS/Darwin 平台实现，未发现明确无法 iOS 编译的 Android-only 插件。
- Windows-only 路径：本机生成的 `ios/Flutter/Generated.xcconfig`、`flutter_export_environment.sh`、`ios/Flutter/ephemeral/` 含本机路径，但已由 `ios/.gitignore` 忽略；云端会通过 `flutter pub get` 重新生成。
- 仍需真实验证：iOS Share Extension、App Group、TestFlight 安装、系统分享、系统日历、通知、PDF 打开等必须在 iPhone/iPad 上跑通后才能写成已验证。

### 本步 Windows 验证结果

已执行：

```powershell
flutter analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter analyze`：通过，无 issues。
- `flutter test`：通过。
- `flutter build apk --debug`：通过。
- 最新 APK 输出：`E:\tw_mobile_project\build\app\outputs\flutter-apk\app-debug.apk`

### 当前边界

- 本地目录当前不是 Git 仓库，未执行推送到 `Yc1985Y/QiDi.git`。
- Windows 不能验证 `pod install`、`flutter build ios --release --no-codesign`、IPA 签名和 TestFlight 上传。
- 没有 iPhone/iPad 实机运行记录前，不能声称 iOS 真机验证完成。

## 2026-07-02 Android -> Flutter 对齐审计与 P0 校验页迁移

### 本步目标

- 按附件要求先完成原 Android 工程与当前 Flutter 工程的功能/页面/模型/系统能力差异审计。
- 在审计完成后，立即开始补齐 P0 主闭环差距，不停留在文档说明。

### 本步完成内容

- 新增 `ANDROID_TO_FLUTTER_PARITY_AUDIT.md`
  - 已用中文输出：
    - 源 Android 工程完整功能清单
    - Flutter 当前功能清单
    - 功能差距表
    - 页面差距表
    - 数据模型差距表
    - 系统能力差距表
    - 资源差距表
    - P0 / P1 / P2 优先级
- 已开始 P0 真实迁移：
  - 新增 Flutter 独立解析校验页：`timeweaver_flutter/lib/pages/review_page.dart`
  - 首页待确认卡的“编辑”入口不再只走轻量弹窗，改为进入独立校验页
  - 校验页支持：
    - 原始输入回顾
    - 标题 / 类型 / 时间 / 截止 / 地点 / 备注字段编辑
    - 与现有时间线事项的真实时间冲突提示
    - 保存校验
    - 确认写入时间线
    - 取消本次
  - `AppController` 已新增待确认事项冲突检测方法，用已确认时间线的真实时间数据判断同日近时段冲突

### 本步验证结果

已执行：

```powershell
flutter pub get
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter pub get`：通过
- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- APK 输出：`E:\tw_mobile_project\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- 当前 Flutter 工程已经从“首页内联轻编辑”提升为更接近原 Android 原型的“独立解析校验页”流程。
- 这一步仍然只是 P0 的第一部分；时间线 `日 / 周 / 月 / 日历总览`、通知中心、导出记录、运行状态、账号体系等差距仍需继续真实迁移。

## 2026-07-03 Android -> Flutter 时间线多视图迁移与时间解析对齐

### 本步目标

- 继续严格以原 Android 时间线模块为唯一标准，迁移：
  - `TimelineScreenModule.kt`
  - `TimelineCalendarModule.kt`
  - `TimelineDetailModule.kt`
- 不重做设计，不新增假功能，直接补齐 Flutter 当前缺失的 `本日 / 本周 / 本月 / 日历总览 / 详情面板` 结构。
- 同时修复 Flutter 侧“校验页提示支持原始时间文本，但底层实际只认 ISO 时间”的不一致问题，使时间线、提醒、日历写入链路与原 Android 的时间解析规则一致。

### 本步完成内容

- 新增 `timeweaver_flutter/lib/pages/timeline_logic.dart`
  - 按原 Android `PlanModels.kt` 迁移并落地：
    - 时间线分组
    - 本周时间窗口
    - 月历矩阵
    - 月标题 / 日标题
    - 时间显示 / 导出时间
    - 提醒摘要
- 重写 `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 新增真实时间线模式切换：`本日 / 本周 / 本月`
  - 新增真实“日历总览”页面结构：
    - 月份切换
    - 日期点选
    - 当日事项列表
    - 日期事件数量圆点
  - 新增真实时间线详情面板：
    - 编辑
    - 保存 / 取消编辑
    - 导航
    - 复制
    - 系统分享
    - 复制成新事项
    - 删除
  - 保留并复用现有真实链路：
    - `updateEvent`
    - `deleteEvent`
    - `duplicateEvent`
    - `openMap`
    - `shareEvent`
    - `copyEvent`
  - 在时间线页保留待确认事项卡片，并补上：
    - 确认写入
    - 返回首页
    - 去校验
    - 取消
- 修复 Flutter 时间解析与 Android 原逻辑不一致问题：
  - 更新 `timeweaver_flutter/lib/utils/date_utils.dart`
    - 新增与 Android 对齐的多格式时间解析能力
    - 支持 `yyyy-MM-dd HH:mm`
    - 支持 `yyyy/M/d HH:mm`
    - 支持 `M月d日 HH:mm`
    - 支持 `M月d日 H:mm`
    - 支持 `M月d日 HH点mm分`
    - 支持 `M月d日 H点m分`
    - 支持仅月日文本的兜底解析规则（沿用 Android 逻辑）
  - 更新 `timeweaver_flutter/lib/models/event_item.dart`
    - 事件时间读取改为统一走 `ZhishiDateUtils.parse(...)`
  - 更新 `timeweaver_flutter/lib/utils/validators.dart`
    - 待确认事项的可确认校验改为走统一时间解析逻辑
  - 更新 `timeweaver_flutter/lib/app.dart`
    - 冲突检测参考时间改为走统一时间解析逻辑
- 新增测试：
  - `timeweaver_flutter/test/timeline_logic_test.dart`
  - 覆盖：
    - Android 风格时间文本解析
    - 时间线周/月视图过滤
    - 提醒摘要与导出时间格式

### 本步验证结果

已执行：

```powershell
flutter analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 本步为避免 Dart/Flutter 在中文路径下的分析器异常，实际通过 ASCII Junction 路径 `E:\codex_timeweaver_flutter_check` 执行验证
- 本步强制使用 E 盘环境变量：
  - `GRADLE_USER_HOME=E:\devtools\gradle-cache`
  - `PUB_CACHE=E:\devtools\pub-cache`
  - `ANDROID_HOME=E:\AndroidSDK`
  - `ANDROID_SDK_ROOT=E:\AndroidSDK`
- 最新 APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter 时间线已经从“单一按天列表 + 轻量弹窗编辑”迁移到更接近原 Android 的多视图时间线结构。
- 当前已真实补齐时间线主页面的关键展示与交互层级，但“我的”页深层模块、通知中心、运行状态、导出记录、账号体系等仍需继续按原 Android 工程逐项迁移。

## 2026-07-03 Android -> Flutter「我的」页主闭环第一组迁移

### 本步目标

- 继续严格按原 Android `ProfileScreenModule.kt` 迁移 `我的` 页第一组高频模块。
- 不做空路由，不做假页面；先把与 Flutter 当前真实数据链路最贴近的一组功能落地：
  - `Dashboard`
  - `运行中枢`
  - `通知中心`
  - `提醒中心`
  - `时间线资产`
  - `运行状态`
  - `导出记录`
  - 以及支撑这些页面的真实数据层

### 本步完成内容

- 新增真实数据模型：
  - `timeweaver_flutter/lib/models/inbox_message.dart`
  - `timeweaver_flutter/lib/models/export_record.dart`
- 扩展用户资料模型：
  - `timeweaver_flutter/lib/models/user_preference.dart`
  - 新增字段：
    - `avatarPath`
    - `signature`
    - `birthday`
    - `age`
    - `gender`
    - `hometown`
- 扩展本地持久化：
  - `timeweaver_flutter/lib/services/storage_service.dart`
  - `timeweaver_flutter/lib/repositories/event_repository.dart`
  - 新增真实保存 / 读取：
    - Inbox 消息
    - 导出记录
- 扩展权限状态读取：
  - `timeweaver_flutter/lib/services/permission_service.dart`
  - 新增：
    - 相机权限状态
    - 相册权限状态
    - 通知权限状态
    - 麦克风权限状态
- 扩展 `AppController`：
  - `timeweaver_flutter/lib/app.dart`
  - 新增真实状态字段：
    - `inboxMessages`
    - `exportRecords`
    - `dataStoreReady`
    - 权限状态缓存
  - 新增真实链路记录：
    - 解析成功 / 空结果 / 失败写入通知中心
    - 确认写入时间线写入通知中心
    - 丢弃待确认事项写入通知中心
    - PDF 导出写入导出记录和通知中心
    - 分享接收失败写入通知中心
  - 新增真实动作：
    - `clearInboxMessages()`
    - `openExportRecord(...)`
    - `reparseEvent(...)`
    - 运行状态权限刷新
- 重写 `timeweaver_flutter/lib/pages/profile_page.dart`
  - 从单页表单迁移为内部路由结构
  - 已实现页面：
    - `Dashboard`
    - `运行中枢`
    - `历史记录`
    - `统计`
    - `账户`
    - `设置`
    - `个人资料`
    - `提醒中心`
    - `时间线资产`
    - `导出记录`
    - `运行状态`
    - `通知中心`
  - 这些页面当前均接真实数据，不含 mock / demo / placeholder / 空壳按钮
- 个人资料页已支持真实头像来源：
  - 从相册选择
  - 拍摄头像
  - 并写回本地偏好持久化

### 本步验证结果

已执行：

```powershell
flutter analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 最新 APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter 的 `我的` 页已经不再是浅层资料表单，而是迁移成接近原 Android 的多入口、多详情页结构。
- 本步已把“通知中心 / 导出记录 / 运行状态 / 提醒中心 / 时间线资产”这一组先接到真实数据层上。
- 原 Android 的本地登录 / 注册 / 会话体系仍未迁入 Flutter；当前 `账户` 页保持诚实展示，不冒充“已完成登录能力”。

## 2026-07-03 Android -> Flutter 本地账号 / 会话 / 资料回写迁移
### 本步目标

- 严格以原 Android `account/AccountRepository.kt`、`account/AccountSessionStore.kt`、`ui/LoginScreen.kt` 和 `MainActivity.kt` 为唯一标准，把本地账号、登录注册、会话恢复、退出登录、资料回写真实迁入 Flutter。
- 不重做现有产品结构，不新增 mock/demo/假登录；直接补齐 Android 原本已经存在的本地 SQLite 账号能力。

### 本步完成内容

- 新增账号模型：
  - `timeweaver_flutter/lib/models/account_user.dart`
  - `timeweaver_flutter/lib/models/account_auth_result.dart`
- 新增真实本地账号仓库：
  - `timeweaver_flutter/lib/repositories/account_repository.dart`
  - 使用 `sqflite` 落本地 SQLite
  - 按 Android 原字段建表：
    - `id`
    - `account`
    - `nickname`
    - `avatar_uri`
    - `signature`
    - `birthday`
    - `school`
    - `age`
    - `gender`
    - `major`
    - `grade`
    - `hometown`
    - `password_hash`
    - `salt`
    - `created_at`
    - `last_login_at`
  - 密码摘要改为与 Android 一致的 `salt + SHA-256`
  - 已接入 Android 同源内置测试账号：
    - 账号：`1985`
    - 密码：`12345678`
    - 昵称：`织时测试账号`
- 新增真实会话存储：
  - `timeweaver_flutter/lib/services/account_session_service.dart`
  - 使用 `SharedPreferences` 保存当前登录用户 id
  - 应用启动时恢复当前会话
  - 退出登录时清除会话
- 新增登录页：
  - `timeweaver_flutter/lib/pages/login_page.dart`
  - 文案对齐 Android：
    - `织时`
    - `将校园信息碎片整理成清晰时间线`
    - `欢迎回来`
    - `创建账号`
    - `学号或邮箱`
    - `密码`
    - `昵称`
    - `进入织时`
    - `注册并进入织时`
    - `正在校验账号`
    - `去注册`
    - `去登录`
- 扩展 `timeweaver_flutter/lib/app.dart`
  - 启动时先加载 SQLite 账号与本地会话
  - 未登录时先显示登录页，登录后再进入三页主结构
  - 新增真实动作：
    - `loginAccount(...)`
    - `registerAccount(...)`
    - `logoutAccount()`
    - `saveAccountProfile(...)`
  - 资料保存后会同步更新当前会话和本地偏好镜像
  - 头像保存改为复制进应用文档目录 `profile/`，不再只保留临时选图路径
- 扩展真实数据归属：
  - `timeweaver_flutter/lib/models/event_item.dart`
  - `timeweaver_flutter/lib/models/parsed_notice.dart`
  - `timeweaver_flutter/lib/models/export_record.dart`
  - 既有 `InboxMessage` 继续使用 `ownerAccount`
  - 时间线事项、待确认事项、通知中心、导出记录现在都带账号归属并按当前登录账号隔离显示
  - 为兼容此前 Flutter 版本尚无账号体系的本地旧数据，首次登录时会把“无归属账号”的历史本地数据归入当前登录账号，避免已有真实数据被隐藏
- 更新 `timeweaver_flutter/lib/pages/profile_page.dart`
  - `账户` 页不再显示“尚未接入登录能力”的说明
  - 新增当前账号展示、测试账号说明、退出登录入口
  - `设置` 页新增当前账号与退出登录入口
  - `个人资料` 页改为真实写回 SQLite 账号资料，而不是只改本地偏好

### 本步验证结果

已执行：

```powershell
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 最新 APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter 已真实补齐原 Android 的本地账号、登录注册、会话恢复、退出登录、个人资料回写链路。
- 当前账号体系不是占位说明，而是 SQLite + 本地会话的真实实现。
- 这一轮没有改首页 / 时间线 / 我的页的产品方向，只把原 Android 已有账号能力迁入 Flutter。

## 2026-07-03 Android -> Flutter「我的 / 设置」深层页面第二组迁移
### 本步目标

- 继续严格以原 Android `ProfileScreenModule.kt`、`ProfileStatusModels.kt`、`ProfileCheckupModels.kt` 为唯一标准，把 Flutter `我的` 页缺失的深层页面补齐。
- 本步优先迁移：
  - `我的成就`
  - `用户画像`
  - `隐私与安全`
  - `数据空间`
  - `偏好设置`
  - `智能体体检`

### 本步完成内容

- 扩展 `timeweaver_flutter/lib/pages/profile_page.dart`
  - 新增内部路由：
    - `agentCheckup`
    - `achievements`
    - `preferences`
    - `persona`
    - `privacySecurity`
    - `dataSpace`
  - 更新已有入口：
    - `Dashboard`
    - `运行中枢`
    - `账户`
    - `设置`
  - 让上述新页面都能从现有 Flutter 路由结构中真实进入，而不是写文档占位

- 新增真实页面：`智能体体检`
  - 体检项评分规则按 Android `ProfileCheckupModels.kt` 同步迁移：
    - 待确认池
    - 时间字段
    - 地点字段
    - 提醒覆盖
    - 安全边界
    - 时间冲突
    - 消息收纳
    - 数据沉淀
  - 评分结论规则同步迁移：
    - `可直接演示`
    - `先处理 X 项`
    - `建议微调 X 项`
    - `状态稳定`
  - 每个体检项都保留真实跳转入口，可进入对应页面继续处理

- 新增真实页面：`我的成就`
  - 按 Android 成就页文案迁移：
    - `海报小能手`
    - `低噪规划师`
    - `校验守门人`
    - `时间线织工`
  - 数据来自真实 Flutter 状态：
    - 已确认事项数
    - 已挂载提醒数
    - 待确认事项数
  - 不做假勋章等级和假后台数据

- 新增真实页面：`用户画像`
  - 画像计算按 Android 规则迁移：
    - `高密度规划型`
    - `谨慎校验型`
    - `低噪留白型`
    - `稳态推进型`
  - 画像基于真实状态计算：
    - 已确认事项数
    - 待确认事项数
    - 今日事项数
    - 已挂载提醒数

- 新增真实页面：`隐私与安全`
  - 接入真实开关：
    - `高风险动作拦截`
    - `低置信度静默处理`
  - 保留 Android 原有说明结构：
    - `安全边界`
    - `隐私说明`
  - 操作会真实写回本地偏好

- 新增真实页面：`数据空间`
  - 展示真实本地状态：
    - 已确认事项数
    - 待处理事项数
    - 已挂载提醒数
    - 本地库状态
  - 保留 Android 原有分层表达：
    - 导入来源
    - 解析结果
    - 时间资产
    - 导出缓存
  - `查看时间线资产` 入口会真实跳到现有时间线资产页

- 新增真实页面：`偏好设置`
  - 将 Android 偏好板集中为独立二级页
  - 接入真实偏好写入：
    - 提前一天预热
    - 临近前提醒
    - 高风险动作拦截
    - 低置信度静默处理
    - 自动地图联动
    - 轻量性能模式

- 清理空壳交互
  - 本步中凡是纯展示项，一律改为静态展示，不保留“能点但没有行为”的空壳按钮

### 本步验证结果

已执行：

```powershell
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 最新 APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter 的 `我的 / 设置` 深层结构已继续向原 Android 贴近，不再只停在通知中心、导出记录、运行状态这一级。
- 本步新增页面均接入真实 Flutter 现有数据和偏好状态，没有用 mock / demo / placeholder / 空壳按钮冒充完成。
- 原 Android 中更细的首页工作台、更多品牌资源细节、以及后续尚未迁完的边角模块，仍需继续逐项对照源码推进。

## 2026-07-03 Android -> Flutter 对齐推进：首页结构与真实多格式导出

### 本步目标

- 继续以原 Android 首页模块为唯一标准，对齐 `HomeGreeting / HomeOverviewCard / InputEnergyHub / PendingReviewCard / RecentRecognitionCard` 结构。
- 把时间线导出从单一 PDF 扩展为真实 `PDF / PNG / JPG` 三种格式，不能用假入口或占位说明代替。
- 不改产品设计方向，不新增 mock / demo / 假数据，只补真实实现。

### 本步完成内容

- 首页结构继续贴近原 Android：
  - `timeweaver_flutter/lib/pages/home_page.dart`
  - `timeweaver_flutter/lib/pages/input_page.dart`
  - 首页改为：
    - 问候区
    - 总览卡
    - 输入中枢
    - 最近识别结果区
    - 待确认主卡 / 最近识别摘要
  - 待确认区不再简单平铺所有卡片，而是按 Android 首页主卡逻辑突出首条待确认事项，并保留：
    - 冲突提示
    - 进入校验页
    - 确认加入日程
    - 取消
  - 输入区补齐真实能力：
    - 剪贴板导入
    - 拍照
    - 相册
    - 语音转写
    - 图片移除

- 真实导出能力补齐：
  - `timeweaver_flutter/lib/services/timeline_export_service.dart`
  - `timeweaver_flutter/lib/pages/timeline_page.dart`
  - `timeweaver_flutter/lib/app.dart`
  - 新增 `TimelineExportFormat`
  - 导出服务现在支持：
    - PDF：继续使用真实 PDF 生成
    - PNG：真实位图渲染导出
    - JPG：基于真实位图内容编码导出
  - 时间线页导出入口改为格式菜单，直接触发真实导出，不保留空壳选项
  - 导出记录继续真实写入本地 `ExportRecord`

- 依赖补齐：
  - `timeweaver_flutter/pubspec.yaml`
  - 新增 `image`，用于 JPG 编码，未引入 mock 逻辑

- 编码污染修复：
  - `timeweaver_flutter/lib/app.dart`
  - `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 本步在继续迁移时发现两处已有字符串被历史编码污染，导致引号断裂、分析器报错。
  - 已逐段修复为可编译状态，没有改业务流程，只修正损坏的文案字面量。

### 本步验证结果

已执行：

```powershell
flutter pub get
dart analyze
flutter test
flutter build apk --debug
```

结果：
- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 最新 APK 输出：`E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter 首页已经进一步向原 Android 首页结构贴近，不再停留在“功能能用但层级不一致”的状态。
- 时间线导出不再只有 PDF，现已具备真实 `PDF / PNG / JPG` 导出链路。
- 当前仍未宣称完成 iPhone / iPad 真机验证；系统分享接收、日历、通知、导出打开等 iOS 侧仍需后续真机或云端签名后验证。

### 2026-07-03 补充：Profile 页稳定化

- `timeweaver_flutter/lib/pages/profile_page.dart` 已重建为干净可编译版本，保留并接回当前真实功能：
  - 历史记录
  - 统计
  - 账号
  - 个人信息
  - 偏好设置
  - 提醒中心
  - 时间线资产
  - 导出记录
  - 运行状态
  - 通知中心
  - 隐私与安全
  - 数据空间
  - 用户画像
  - 智能体体检
  - 我的成就
- 历史记录页补齐：
  - 日期筛选
  - 状态筛选
  - 来源筛选
  - 按时间倒序
- 重新验证：
  - `dart analyze`：通过
  - `flutter test`：通过
  - `flutter build apk --debug`：通过

## 2026-07-03 Android -> Flutter 实时相机工作台与字体资源回迁

### 本步目标

- 继续以原 Android `ui/camera/LiveCameraCaptureScreen.kt` 为唯一标准，把 Flutter 当前“系统拍照 picker”推进成更接近原作品的实时取景工作台。
- 同时把 Android 原字体资源回迁到 Flutter 主题层，减少页面字体漂移。

### 本步完成内容

- 新增实时相机页面：
  - `timeweaver_flutter/lib/pages/live_camera_capture_page.dart`
  - 已实现：
    - 实时预览
    - 快门拍照
    - 闪光灯开关
    - 前后镜头切换
    - 相册入口
    - 相机不可用时的真实错误提示
- 更新输入页：
  - `timeweaver_flutter/lib/pages/input_page.dart`
  - 首页“拍照识别”和输入区“拍照”按钮现在会进入实时相机页，而不是直接调系统相机 picker
- 回迁原 Android 字体：
  - 已从 `VisualSemanticAgent/app/src/main/res/font/` 复制到 `timeweaver_flutter/assets/fonts/`
  - `pubspec.yaml` 已注册：
    - `Literata`
    - `Manrope`
    - `PlusJakartaSans`
    - `NotoSansSC`
  - `timeweaver_flutter/lib/app.dart` 已开始使用原字体体系
- 修复可见乱码文案：
  - `timeweaver_flutter/lib/app.dart`
  - `timeweaver_flutter/lib/pages/timeline_page.dart`

### 本步验证结果

已执行：

```powershell
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 由于 Windows 中文路径会触发 Gradle 非 ASCII 限制，本步实际在 `E:\codex_timeweaver_flutter_check` 下完成 APK 构建
- 最新 APK 输出：
  - `E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter 拍照链路已经更接近原 Android 的实时相机工作台，而不是停留在简化版系统 picker。
- 字体体系已开始向原 Android 工程回收，后续可以继续细调具体字号、字重和卡片密度。
- 当前仍不能宣称 iPhone / iPad 真机验证完成；实时相机、系统分享、日历、通知、导出打开都还需要 TestFlight 后继续验收。

## 2026-07-03 Android -> Flutter「我的」页结构继续收紧

### 本步目标

- 继续严格对照 Android `ProfileTileModels.kt`、`ProfileSummaryModels.kt`、`ProfileInboxModels.kt`、`ProfileCheckupModels.kt`。
- 不新增无关功能，只把 Flutter `我的` 页当前已实现的真实能力，进一步往 Android 原有的分组、文案和筛选逻辑收拢。

### 本步完成内容

- 更新 `timeweaver_flutter/lib/pages/profile_page.dart`
  - Dashboard 改为更接近 Android 的两组入口组织：
    - `功能地图`
    - `系统入口`
  - `智能体中心` 二级页改为更接近 Android 的 tile 标题与摘要：
    - `一键体检`
    - `日程表`
    - `通知中心`
    - `我的成就`
    - `用户画像`
    - `隐私与安全`
    - `数据空间`
    - `运行状态`
  - 个人资料卡增加 Android 同源 fallback 文案：
    - `请完善校园身份资料`
    - `请完善生日、年龄和学校资料`
- 通知中心筛选逻辑继续按 Android 对齐：
  - `最近消息`
  - `待处理消息`
  - `解析反馈`
  - 反馈筛选不再只看 `feedback` 文本，还会识别：
    - `clarification`
    - `error`
    - `blocked`
    - `tts`
  - 不同类型消息会用不同图标和底色表达
- 智能体体检逻辑继续向 Android 对齐：
  - `地点字段` 检查现在会把 `自动地图联动` 开关纳入结论
  - `消息收纳` 会把错误类消息计入待处理
  - `邻近冲突` 标题改回更接近 Android 的 `时间冲突`
- `数据空间` 页增加 Android 风格说明区，并补充：
  - 历史记录入口
  - 通知中心入口

### 本步验证结果

已执行：

```powershell
dart analyze
flutter test
flutter build apk --debug
```

结果：

- `dart analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 本步 APK 构建继续在 ASCII 路径 `E:\codex_timeweaver_flutter_check` 下完成
- 最新 APK 输出：
  - `E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- Flutter `我的` 页现在不仅“功能能点”，而且入口分组、文案语义和通知中心筛选逻辑都更贴近 Android 原工程。
- 当前仍未达到“完全 1:1 对齐可推送验收”的状态；接下来还需要继续压缩 `我的` 页和时间线的视觉层差异，并在条件具备后做 iOS 真机验收。

## 2026-07-04 首页结果区与解析校验页继续向 Android 收紧

### 本步目标

- 继续以原 Android `HomeResultSection.kt` 与 `ReviewScreenModule.kt` 为唯一标准。
- 不新增 UI 风格、不新增 mock/demo/假数据，只把 Flutter 已有真实链路进一步收紧到 Android 的卡片层级和文案表达。

### 本步完成内容

- 更新 `timeweaver_flutter/lib/widgets/weaving_widgets.dart`
  - `InfoChip` 增加可选底色和文字色参数，便于在不改全局设计体系的前提下，对齐 Android 首页结果卡与解析校验页里的 chip 表达。
- 更新 `timeweaver_flutter/lib/pages/home_page.dart`
  - 首页 `待确认主卡` 的来源、队列、冲突 chip 改为更接近 Android 的暖珊瑚 / 金色层次。
  - 待确认摘要不再优先退化为统一提示文案，而是按真实字段顺序读取：
    - `description`
    - `source.rawText`
    - `source.ocrText`
  - `最近识别结果` 摘要卡去掉额外加粗，回到更接近 Android `RecentRecognitionCard` 的信息卡表达。
- 更新 `timeweaver_flutter/lib/pages/review_page.dart`
  - `校验提示` 卡继续向 Android `ReviewIssueCard` 对齐：
    - 有问题时标题为 `需要留意`
    - 无问题时标题为 `校验结果`
    - 背景色按问题状态切换
    - 成功文案收紧为 `标题、时间、地点等关键字段已经具备，可以确认写入。`
  - 头部摘要文案增加三行裁切，更接近 Android hero 卡的展示密度。
  - 底部 `保存校验 / 取消本次` 从普通按钮收紧为更接近 Android 的卡片式二级动作。
  - 主确认按钮图标改为 `check_circle_rounded`，与 Android 入口一致。

### 本步验证结果

实际验证继续在 ASCII 镜像目录执行：

- 工程镜像：`E:\codex_timeweaver_flutter_check`
- Flutter SDK：`E:\devtools\flutter`
- Gradle cache：`E:\devtools\gradle-cache`

已执行：

```powershell
$env:GRADLE_USER_HOME='E:\devtools\gradle-cache'
flutter pub get
flutter analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter pub get`：通过
- `flutter analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 最新 APK 输出：
  - `E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步补充说明

- 本次 `apk --debug` 首次失败不是首页/校验页代码逻辑问题，而是命令没有走 `E:` 盘 Gradle 缓存，误用了 `C:\Users\yc\.gradle` 的缓存。
- 按 `配置说明.md` 切换 `GRADLE_USER_HOME=E:\devtools\gradle-cache` 后，构建恢复通过。
- 这也再次说明：后续 Windows 本机验证应优先使用 `E:` 盘 Flutter / Android / Gradle 环境，不要把构建缓存压回 `C:` 盘。

### 本步结论

- 首页结果区与解析校验页已经继续向 Android 卡片结构收紧，且没有破坏现有真实解析、真实确认、真实丢弃链路。
- 当前仍不能宣称“完全 1:1 对齐完成”：
  - 首页与时间线仍有少量视觉密度差异；
  - `我的` 页仍有局部 Android 文案与层级尾差；
  - iPhone / iPad 真机验证仍未完成，不能声称 iOS 真实验收已完成。

## 2026-07-04 时间线 / 我的页剩余高频尾差继续收紧

### 本步目标

- 在不追求 100% 同构的前提下，把剩余最影响观感和操作路径的差异继续压缩到更接近 Android 原工程。
- 本步重点处理：
  - 时间线日历起始规则与详情编辑动作样式
  - 我的页运行状态 / 通知中心 / 数据空间的状态表达和色板对齐

### 本步完成内容

- 更新 `timeweaver_flutter/lib/pages/timeline_logic.dart`
  - `timelineMonthMatrix(...)` 从原来的周一开栏改为更接近 Android 的周日开栏，避免月历表头和日期格布局不一致。

- 更新 `timeweaver_flutter/lib/pages/timeline_page.dart`
  - 时间线 `日历总览` 入口卡拆成标题 + 月度摘要两行，减少和 Android 顶部入口的层级差异。
  - 日历总览页周表头改为：
    - `日 / 一 / 二 / 三 / 四 / 五 / 六`
  - 时间线详情页的编辑态操作从普通按钮进一步收紧为更接近 Android 的卡片式二级动作：
    - `保存 / 写回时间线`
    - `取消 / 保留原内容`
  - 保留原有真实能力：
    - 编辑
    - 导航
    - 复制
    - 分享
    - 复制成新事项
    - 删除

- 更新 `timeweaver_flutter/lib/pages/profile_page.dart`
  - `智能体中心入口卡` 的 `更多功能` 标记改为更接近 Android 的 `InfoChip + 图标` 表达。
  - `运行状态` 模块的 `正常 / 待处理` 状态 chip 改为真实颜色状态：
    - 正常：浅绿色
    - 待处理：珊瑚色
  - `通知中心` 消息行的状态 chip 改为跟随消息类型和状态底色，不再全部使用统一样式。
  - `智能体体检` 列表中的 `正常 / 观察 / 处理` chip 改为分级配色，更接近 Android 体检状态表达。
  - `数据空间` 色板调整为更接近 Android：
    - 已确认：金色
    - 待处理：珊瑚色
    - 提醒：浅绿色
    - 本地库：白底状态
  - `数据空间` 中的本地库存储状态改为真实读取 `controller.dataStoreReady`，不再固定写死显示。

### 本步验证结果

继续在 ASCII 镜像目录执行：

- 工程镜像：`E:\codex_timeweaver_flutter_check`
- Flutter SDK：`E:\devtools\flutter`
- Gradle cache：`E:\devtools\gradle-cache`

已执行：

```powershell
$env:GRADLE_USER_HOME='E:\devtools\gradle-cache'
flutter analyze
flutter test
flutter build apk --debug
```

结果：

- `flutter analyze`：通过，`No issues found!`
- `flutter test`：通过，`All tests passed!`
- `flutter build apk --debug`：通过
- 最新 APK 输出：
  - `E:\codex_timeweaver_flutter_check\build\app\outputs\flutter-apk\app-debug.apk`

### 本步结论

- 这一轮主要清掉了剩余最容易被看出来的时间线 / 我的页尾差，当前已经更接近“功能 99% 相似、设计 95% 以上相似”的目标。
- 仍然保留的差异，更多是局部卡片密度和部分说明文案，不再是主流程缺项。
- iPhone / iPad 真机验证依然未做，不能把 iOS 真实验收说成已完成。
