# 《织时》作业提交说明

## 提交内容

本次作业提交建议包含两类材料：

- 可安装 APK：用于评审或老师直接安装体验。
- 完整 Android 工程压缩包：用于项目源码提交、复现构建和后续验收。

独立交付目录：

```text
D:\桌面\本科\课题all\中国高校计算机大赛AIGC赛道_交付物
```

## APK

主要提交 APK：

```text
D:\桌面\本科\课题all\中国高校计算机大赛AIGC赛道_交付物\实机验证APK\织时-app-debug.apk
```

说明：

- 该 APK 来自 `app/build/outputs/apk/debug/app-debug.apk` 的单独复制版本。
- Debug APK 已成功安装到 `emulator-5554` 并启动。
- 包名：`com.vsa.visualsemanticagent`。
- 桌面名称：`织时`。
- 如果只要求提交一个 APK，优先提交这份 `织时-app-debug.apk`，因为它已经完成安装启动验证，且放在固定交付目录中，便于直接分发到真机。

## 完整工程

完整工程压缩包：

```text
VisualSemanticAgent（完整工程目录，按需自行打包）
```

说明：

- 压缩包包含当前 `VisualSemanticAgent` Android 工程。
- 包含源码、Gradle 配置、文档、阶段汇报材料和当前已采用的 vivo 大模型 API 接入配置。
- 不包含 Gradle 构建缓存、IDE 临时缓存、安装产物目录等可重新生成内容。

## 测试账号

```text
学号：1985
密码：12345678
```

## 已验证状态

本轮已完成：

- `:app:assembleDebug` 构建通过。
- `:app:connectedDebugAndroidTest` 在 `VSA_API34_GOOGLE / emulator-5554` 上通过，22 项仪器测试全部成功。
- Debug APK 已安装并启动到前台。
- vivo 大模型 API 与 OCR 接入代码未移除，主流程未回退 mock。

补充说明：

- JVM 单元测试 `:app:testDebugUnitTest` 在当前 Windows + AGP 环境下存在 JUnit 测试类发现异常，错误为 `ClassNotFoundException: com.vsa.visualsemanticagent.ModuleTests`。该问题发生在测试运行器加载阶段，不是业务逻辑断言失败；当前交付以已通过的 Android 仪器测试和 APK 实机/模拟器安装启动为准。

## 推荐打开工程环境

JDK：

```text
E:\AIGC\tools\jdk17\jdk-17.0.19+10
```

Android SDK：

```text
E:\AndroidSDK
```

Android Studio：

```text
E:\Program Files\Android\Android Studio\bin\studio64.exe
```

模拟器：

```text
VSA_API34_GOOGLE
```

远端仓库：

```text
https://gitee.com/hr0503/vivo.git
```
